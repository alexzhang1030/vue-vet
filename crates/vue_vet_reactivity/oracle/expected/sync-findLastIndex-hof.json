{
  "id": "sync-findLastIndex-hof",
  "source": "import { ref, computed } from 'vue'\nconst list = ref([1, 2, 3])\nconst target = ref(2)\nconst index = computed(() => list.value.findLastIndex((n) => n === target.value))\nvoid index.value\n",
  "runtime_deps": [
    {
      "binding": "list",
      "key": "value"
    },
    {
      "binding": "target",
      "key": "value"
    }
  ]
}

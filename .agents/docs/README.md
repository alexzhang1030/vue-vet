# Vue Vet context map

Open the record routed for the code or decision you are about to change. These records hold durable project judgment; temporary execution state belongs in GitHub issues and the root roadmap.

| When changing | Read | Why |
| --- | --- | --- |
| Product scope, target users, scoring intent, or release promises | [goal](./goal.md) and [root roadmap](../../ROADMAP.md) | Defines what Vue Vet is trying to become and what it deliberately refuses before Beta. |
| Dependencies, tool ownership, Rust-only constraints, or version pins | [technology stack](./technology-stack.md) and [Vize compatibility baseline](../../docs/vize-compatibility.md) | Explains why Vize and Oxc own the semantic stack, why analysis dependencies are pinned, and how Vize upgrades are validated. |
| Crate boundaries, data flow, diagnostics, project graph, reporters, or public APIs | [architecture](./architecture.md) (`Monorepo analysis pipeline`, crate layouts, `vue_vet_reactivity` `ModuleSource` vs `::oxc`), [workspace crates index](../../docs/crates.md), and [analysis-stack ADR](../../docs/adr/0001-analysis-stack.md) | End-to-end stage map; per-crate README; Oxc-taking APIs stay under `vue_vet_reactivity::oxc`; stable Vue Vet-owned boundaries around third-party parsers. |
| Semantic IR layers (`ModuleSummary`, file-rule digests, fact boundaries) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) | Small domain IRs — not a unified AST; file-rule reuse is digest-keyed. |
| Demand-gated source contracts (`customRef` / stopped `effectScope.run` / missing `toRefs` key / cached-result demand / reactive private-field receivers / same-instance inject / VueUse ignore-window and shared first-instance args / cancelled `useDebounceFn` promise / JSON clone Date loss / history snapshot alias) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`, `Native private fields brand the raw instance`) | Function-level region + source-order barriers; class-symbol/member index joined to per-object operations; generic source5 stays `ExpressionStatement` parents. Cached-result and VueUse demand facts require exact `@vueuse/core` / `@vueuse/shared` provenance. Fresh native `Symbol()` is first-slice ancestor-absence proof. Cancelled-filter await is a required settlement boundary, not a source5 barrier. `useCloned` / `useManualRefHistory` are `@vueuse/core` only. |
| Native `Map` raw/proxy key demand (`no-raw-proxy-map-key`) or keyed `forEach`→`get` practice (`prefer-keyed-map-dependency`) | [architecture](./architecture.md) (`Semantic IR layers`), [conventions](./conventions.md), [gotchas](./gotchas.md) (`Native Map keys are not Vue-normalized`) | Fresh unresolved `Map` constructor + actual Vue proxy origin; wrappers sharing one raw allocation share capability; replay mutating ops once per root; CRLF fixtures need `.gitattributes -text`. |
| Dirty-set / execution locality (`ChangeImpact`, `DirtyPlan`, work counters) | [architecture](./architecture.md) (`Dirty-set scheduling`, `Post-#107 locality gap`, `Single-file algorithms`), [gotchas](./gotchas.md) (`Template-only SFC edits re-run script analysis`) | Dirty parse + warm linking/layer caches shipped; prove with counters. Context epochs must not force re-parse. No generalized unified AST IR. Template-ref demand joins force `can_reuse_script = reuse_template && reuse_*`. |
| Lost-notification collection / native-size work counters | [reactivity tracer](./reactivity-tracer.md) (`Current baseline`), [gotchas](./gotchas.md) (`Production notification work counters miss native-size`) | Production `WorkCounter` is zero-sized; `LAST_WORK` is test-only. Preflight uses `imported_bindings` plus the unresolved-reference index; type-only named specifiers stay eligible. Module summaries build one canonical import map and borrow it through local/composable helpers. Empty composable-definition maps leave the call-use walk unused. Each platform gate uses its own artifact. |
| Watch-API source-contract eligibility | [architecture](./architecture.md) (`Semantic IR layers`), [reactivity tracer](./reactivity-tracer.md) (`Current baseline`) | Named effect-family imports keep source indexes empty when every resolved reference is a proven call with fewer than two arguments and no spread. Eligibility charges import-map entries, ancestor hops, wrapper peels, and remaining argument candidates. Production source-contract `WorkCounter` is zero-sized. Ordinary `watch` also runs callback-contract collectors. |
| Reactivity graph facts, tracking scopes, module summaries, tracer completeness, or CLI reactivity digest | [reactivity tracer](./reactivity-tracer.md) (`Current baseline`, `Product stance`; `REACTIVITY_GRAPH_VERSION = 41`, `RULESET_VERSION = 41`), [gotchas](./gotchas.md) (`Typed ref parameters`, `Effect run counts are not onTrack JSON`, `toRef` live `__v_isRef`), [literature matrix](./research/reactivity-tracer-literature.md), [science memo](./research/reactivity-tracer-science.md) (post-v27 ranked next work), [oracle](../../crates/vue_vet_reactivity/oracle/README.md), [JSON output](../../docs/json-output.md), issue [#140](https://github.com/alexzhang1030/vue-vet/issues/140) | Static tracer goals; CLI/JSON digest proves tracer ran even when score is clean; typed `ComputedRef` params classify `.value`. After A0–A7 complete, rank dual-path / measurement / locality work from the science memo, not a new axis. Run-count claims use `just oracle-self-trigger`, not onTrack JSON. |
| Rule IDs, fixtures, source spans, deterministic output, configuration, or completion checks | [conventions](./conventions.md) (runtime evidence for loop/run-count premises; watch-callback and normalization contracts), [rule catalog](../../docs/rules/README.md) (`tracer` / `parity` / `practice` tiers), [gotchas](./gotchas.md) (`Collection escape-depth exhaustion is Unknown`, `customRef lost-notification needs an executed, still-active consumer`) | Records deliberate engineering rules that contributors and agents must preserve; catalog lists every registered id and differentiation tier. Helper-argument leftover spans at escape-depth exhaustion stay Unknown, including unresolved native constructor/prototype identifiers, const constructor/prototype aliases of those identities, and true `globalThis` alias constructor taint. |
| Watcher EventTarget cleanup identity (`no-watch-cleanup-current-source`) | [gotchas](./gotchas.md) (`Watch cleanup identity needs an executed schedule`), [architecture](./architecture.md) (lifetime facts), [rule essay](../../docs/rules/reactivity/no-watch-cleanup-current-source.md) | Acquisition before a later distinct allocation; default pre coalescing is a safe control. |
| Nested watch / detached `effectScope` ownership (`no-nested-watch-without-cleanup`, `no-detached-effect-scope-without-stop`) | [architecture](./architecture.md) (lifetime facts), [rule essays](../../docs/rules/reactivity/no-nested-watch-without-cleanup.md) | Shared lifetime index; discarded inner watches and detached scopes without stop. |
| Late cancellation guard (`no-late-cancellation-guard`) | [architecture](./architecture.md) (lifetime facts), [gotchas](./gotchas.md) (`once: true` is not a late cancellation-guard window), [rule essay](../../docs/rules/reactivity/no-late-cancellation-guard.md) | Bound `onCleanup` / `onWatcherCleanup` registered after a source-dependent `await` leaves a stale-settlement window. Abstains on `once: true` or non-closed watch options. Runtime premises: `just oracle-stale-settlement`. |
| customRef track/trigger notification chain (`no-custom-ref-lost-notification`) | [gotchas](./gotchas.md) (`customRef lost-notification needs an executed, still-active consumer`), [architecture](./architecture.md) (`Semantic IR layers`), [rule essay](../../docs/rules/reactivity/no-custom-ref-lost-notification.md) | Closed-local track/trigger reachability with executed consumer, identity setter transfer, and per-handle inactivity queries. |
| Cached-result demand (`no-memoize-stale-result-demand`, `no-controlled-computed-stale-result-demand`) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`) | Exact `@vueuse/core` / `@vueuse/shared` provenance; ordered producer/fill/write/repair/demand fold; native prototype mutations collected before identifier-root early returns. |
| Stable computed identity (`prefer-stable-computed-identity`) | [architecture](./architecture.md) (`Semantic IR layers`), [reactivity tracer](./reactivity-tracer.md) (`Current baseline`), [rule essay](../../docs/rules/practice/prefer-stable-computed-identity.md) | Vue `computed` returning a fresh own-data object/array of primitive projections; later same-owner source replacement keeps Object.is-equal contents; an established identity consumer (`watch(derived)` / downstream computed) repeats work. `computed` is `ContractSink::Computed`. Runtime premises: `just oracle-computed-identity`. |
| One-way `syncRef` / conditional watch source (`prefer-sync-ref-one-way`, `prefer-conditional-watch-source`) | [architecture](./architecture.md) (`Semantic IR layers`), [reactivity tracer](./reactivity-tracer.md) (`Current baseline`), [rule essays](../../docs/rules/practice/prefer-sync-ref-one-way.md) | VueUse `syncRef` whose right side is a closed sink, and array-source `watch` whose callback is guarded by a discriminator ref. `syncRef` is `ContractSink::SyncRef`. Runtime premises: `just oracle-derivation-practice`. |
| Queued watch flush / attached child scope / lazy `computedAsync` (`prefer-queued-watch-flush`, `prefer-attached-effect-scope`, `prefer-lazy-computed-async`) | [architecture](./architecture.md) (`Semantic IR layers`), [reactivity tracer](./reactivity-tracer.md) (`Current baseline`), [conventions](./conventions.md) | Scheduling-practice facts on `SourceContractFacts.scheduling_practice`; queued or attached delivery vs retained baseline; `just oracle-scheduling-practice`. |
| Until timeout unmatched demand (`no-until-timeout-unmatched-demand`) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`until(source)` is the same generic source5 escape), [rule essay](../../docs/rules/reactivity/no-until-timeout-unmatched-demand.md) | Proven `@vueuse/core` / `@vueuse/shared` `until(ref).toBe` with timeout; closed primitive source; unmatched current value demanded as the expected kind. Runtime premises: `just oracle-until-demand`. |
| Same-instance injection demand (`no-inject-same-instance-provide`) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`), [rule essay](../../docs/rules/reactivity/no-inject-same-instance-provide.md) | Fresh native `Symbol()` key in setup; local `provide` does not supply same-instance `inject`; fallback fails a later native callable demand. Runtime premises: `just oracle-injection-demand`. |
| VueUse ignore-window / shared first-instance args (`no-ignorable-async-ignore-window`, `no-shared-composable-first-instance-args`) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`), [rule essays](../../docs/rules/reactivity/no-ignorable-async-ignore-window.md) | Exact `@vueuse/core` / `@vueuse/shared` `watchIgnorable` sync ignore windows and `createSharedComposable` / `createGlobalState` first-instance argument demands. Runtime premises: `just oracle-vueuse-demand`. |
| Snapshot-demand JSON clone / history alias (`no-json-clone-lossy-type`, `no-ref-history-snapshot-alias`) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`), [rule essays](../../docs/rules/reactivity/no-json-clone-lossy-type.md) | Exact `@vueuse/core` `useCloned` JSON Date loss and `useManualRefHistory` identity dump/parse alias. Runtime premises: `just oracle-snapshot-demand`. |
| Model-default demand (`no-model-default-unsynced-parent-demand`, `no-shared-default-cross-instance-demand`) | [architecture](./architecture.md) (`vue_vet_project` pipeline), [gotchas](./gotchas.md) (`Element start-tag spans cannot prove nesting`), [rule essays](../../docs/rules/reactivity/no-model-default-unsynced-parent-demand.md) | Child `defineModel({ default })` joined onto parent instance demands; kebab-case tags follow `ComponentUsage`. Runtime premises: `just oracle-model-demand`.
| Rule groups, `--list-rules`, `--group` selection, inventory vs scan-time enabling | [architecture](./architecture.md) (`vue_vet_session` group table), [conventions](./conventions.md), [JSON output](../../docs/json-output.md) (`--list-rules`) | Product groups live in session; core DTOs only; effective config `off` for non-selected IDs. Composed inventory is 151 IDs; source-contracts maps 46; lifetime maps 10. || Shared rule fact queries (setup blocks, after-await calls, prior unconditional reads) | [architecture](./architecture.md) (`vue_vet_rule_query`), [vue_vet_rule_query README](../../crates/vue_vet_rule_query/README.md), [conventions](./conventions.md) | Workspace-internal query crate over Vue Vet facts; not a rule pack and not a published core contract. |
| Ecosystem practice suggestions (`recommendation`, VueUse recipes, score exclusion) | [architecture](./architecture.md) (`vue_vet_practice`), [goal](./goal.md), [JSON output](../../docs/json-output.md) | Practice channel is fact-driven recipes in a dedicated crate; not a parallel pattern engine. |
| Safe edit producers, fix preview/application, atomic writes, or rollback | [architecture](./architecture.md), [conventions](./conventions.md), [edit model](../../docs/edit-model.md), and [gotchas](./gotchas.md) | Defines how edits stay attached to active findings, validate before mutation, and fail closed at the current single-file limit. |
| Vize upgrades, offsets, SFC surface extraction, parallel pattern engines, or score stability | [gotchas](./gotchas.md) | Lists traps already identified and the evidence required before changing the current approach. |
| npm launcher, native binaries, release tags, or install matrix | [technology stack](./technology-stack.md), [architecture](./architecture.md), [gotchas](./gotchas.md) (`Keep Cargo targets local to one worktree`), and [install docs](../../docs/install.md) | Thin JS launcher only; platform packages, verified build identities, worktree-local targets, and release/rollback rules. |
| pkg.pr.new continuous preview publishes | [technology stack](./technology-stack.md), [install docs](../../docs/install.md) (`Continuous preview packages`) | Matrix-build `@vue-vet/*` into `dist/`; publish via locked `npm/pkg-pr-new`; GitHub App required. |
| Thin VS Code reactivity visualization (decorations / TreeView) | [reactivity tracer](./reactivity-tracer.md), [JSON output](../../docs/json-output.md), and [`editors/vscode/README.md`](../../editors/vscode/README.md) | Editor host shells out to CLI JSON; no analysis logic in TypeScript. Explain Scope uses workspace `file:@offset` and the disk cache. Full LSP remains issue #12. |
| CLI `--explain` / rule or finding documentation | [JSON output](../../docs/json-output.md) (`--explain`), [architecture](./architecture.md) (stable docs path), issue [#12](https://github.com/alexzhang1030/vue-vet/issues/12) | Rule id early-exit; finding id scans then matches opaque `diagnostics[].id` and nests rule docs. Not the LSP yet. |
| CLI `--explain-scope` / “would Vue re-run?” | [reactivity tracer](./reactivity-tracer.md) (Explain Scope), [JSON output](../../docs/json-output.md) (`--explain-scope`), `vue_vet_reactivity::explain_tracking_scope` | Binding / `module:binding` / `@offset` (start, else covering) query; module-qualified forms skip other graphs; `ScopeExplain` text+JSON; finding `--explain` may nest `tracking`; MCP `vue_vet_explain_scope` is the same JSON; LSP hover and the thin VS Code command use `file:@offset`. |
| Project session / shared scan+explain for CLI·LSP·MCP | [architecture](./architecture.md) (`vue_vet_session`), [JSON output](../../docs/json-output.md), issue [#12](https://github.com/alexzhang1030/vue-vet/issues/12) | Long-lived session owns config, cache, analyze, explain, workspace path bounds; adapters stay thin. |
| Thin diagnostics LSP (`vue-vet --lsp`) | [architecture](./architecture.md) (`vue_vet_lsp`), issue [#12](https://github.com/alexzhang1030/vue-vet/issues/12) | Stdio LSP over session; overlays + versioned publish; safe quick-fix code actions; finding id in `data`; hover is `--explain-scope` `file:@offset`. |
| Thin MCP agent tools (`vue-vet --mcp`) | [architecture](./architecture.md) (`vue_vet_mcp`), issue [#12](https://github.com/alexzhang1030/vue-vet/issues/12) | Stdio JSON-RPC tools: scan, explain, explain-scope, safe-fix preview; scan JSON ships CLI `reactivity` totals; workspace path bounds; never applies edits. |
| Props member inspect (`props.count`) or binding picker UX | [reactivity tracer](./reactivity-tracer.md) (graph v7+ `property` / `to_path`; v8 module-qualified `to_id`) and [JSON output](../../docs/json-output.md) (`binding_nav`) | Digest carries member path. `binding_nav` indexes inbound/outbound/properties; TUI/VS Code look up and scan only as fallback. |
| Vue JSX/TSX (`.jsx`/`.tsx`, SFC `lang=jsx|tsx`), render scopes, JSX→`TemplateFacts` | [architecture](./architecture.md) (`Semantic IR layers`), [reactivity tracer](./reactivity-tracer.md) (Render / graph v9), [gotchas](./gotchas.md) (`SFC compiler macros are setup-only`), issues [#134](https://github.com/alexzhang1030/vue-vet/issues/134) / [#138](https://github.com/alexzhang1030/vue-vet/issues/138) | Oxc-owned JSX surface; no Babel transform; structure-first render recognition + same-file factory forwarders; SFC-macro rules stay on `ScriptKind::Setup`. |
| Cross-file `:foo` → child `props.foo` | [reactivity tracer](./reactivity-tracer.md) (A6 Prop edges) | Static bare-identifier binds only; structural `component_nav` is not prop dataflow. |
| Component uses / used_by navigation | [project graph](../../docs/project-graph.md) (`Component navigation`) and [JSON output](../../docs/json-output.md) (`component_nav`) | Structural `ComponentUsage`/`AutoComponent` only — not prop dataflow. |
| CLI project walks, cache keys, or `node_modules` / symlink installs | [gotchas](./gotchas.md) (`Package directories can look like source files`, `AnalysisStackIdentity` / compat-matrix), [vue_vet_cache README](../../crates/vue_vet_cache/README.md) | Skip `node_modules`; only read paths that are files after symlink resolution. Cache keys hash `CACHE_VIZE_CROQUIS_VERSION` / `CACHE_OXC_PARSER_VERSION` / `OXC_RESOLVER_VERSION` via `AnalysisStackIdentity::current()`; `just compat-matrix` gates those against pin + lock. |
| CLI `--progress` / scan stage streaming | [gotchas](./gotchas.md) (`Scan progress vs per-file stream`), [technology stack](./technology-stack.md) | stderr-only live status or compact phase log; `auto` = TTY stderr and not `CI`; text diagnostics batch after the scan; never pollute JSON stdout. |
| Project import resolution, scoped packages, tsconfig / `.nuxt` paths, Nuxt / Vite auto-import maps | [project graph](../../docs/project-graph.md) (`CONVENTIONS_VERSION = 18`, type-vs-runtime follow), [gotchas](./gotchas.md), [technology stack](./technology-stack.md) | `oxc_resolver` bundler resolution; Nuxt component naming (`.client`, path prefix, `.nuxt` dts); bare `.nuxt/imports.d.ts` + Vite `auto-imports.d.ts` reactivity seeds; no Vite/Nuxt config execution. |
| `vue_vet_project` crate layout / graph pipeline stages | [architecture](./architecture.md) (`vue_vet_project` pipeline), [project graph](../../docs/project-graph.md) | Thin `lib` façade; `pipeline` orchestrates `context` → `structural` → `passes` → trace → `layers` → `rules`. |
| Analysis enrichment passes (Nuxt seeds, provisional Factory / companion `.js`) vs diagnostic Rule passes vs user plugins | [architecture](./architecture.md) (`vue_vet_project` pipeline), [gotchas](./gotchas.md), ADR [0001](../../docs/adr/0001-analysis-stack.md) | Named `Pass::run` + `ENRICHMENT_STEPS`; not AST Traverse; no JS plugin host / empty metadata trait. |
| Reactivity ecosystem API bags (Nuxt data, vue-i18n `t()`, …) | [architecture](./architecture.md) (`Reactivity tracer plugins`), [reactivity tracer](./reactivity-tracer.md), [vue_vet_plugins README](../../crates/vue_vet_plugins/README.md), [install](../../docs/install.md) (`Library crates`) | Compile-time `TracerPlugin` / `NamedApiBag`; published crate; product auto-load; engine free of ecosystem names. |
| Current implementation order or delivery status | [GitHub tracker](https://github.com/alexzhang1030/vue-vet/issues/14) and [root roadmap](../../ROADMAP.md) | Issues are the live execution plan; the roadmap records milestone intent and release gates. |
| Precision corpus, performance scan modes, or release quality gates | [quality gates](../../docs/quality-gates.md), [quality baselines](../../docs/quality-baselines.md), [conventions](./conventions.md) (CodSpeed/Codecov), [gotchas](./gotchas.md) (`cargo bench --profile release` uses panic=unwind), issue [#13](https://github.com/alexzhang1030/vue-vet/issues/13) | Checksummed corpus, precision labels, scan-mode benches, `compat-matrix`; release blocks on quality gates. `cargo bench --profile release` still unwind; shipped CLI is abort. |
| Vue Vapor component-migration assessment, 3.6 compiler/runtime tuple, or vapor-migration research oracles | [technology stack](./technology-stack.md) (`Vue Vapor migration research`), [vapor-migration research](../../docs/research/vapor-migration.md), [`research/vapor-migration`](../../research/vapor-migration/README.md) | Exact `3.6.0-rc.7` + plugin-vue `6.0.8` identity; shipped rule delta 0; JavaScript oracles stay out of the Rust engine. |

Related records should be cross-linked. When a new record or important heading is added, update this map in the same change.

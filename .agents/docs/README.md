# Vue Vet context map

Open the record routed for the code or decision you are about to change. These records hold durable project judgment; temporary execution state belongs in GitHub issues and the root roadmap. Heading names in parentheses are the anchors to read.

| When changing | Read | Why |
| --- | --- | --- |
| Product scope, target users, scoring intent, or release promises | [goal](./goal.md), [root roadmap](../../ROADMAP.md) | What Vue Vet is trying to become and what it refuses before Beta. |
| Dependencies, tool ownership, Rust-only constraints, or version pins | [technology stack](./technology-stack.md), [Vize compatibility baseline](../../docs/vize-compatibility.md), [gotchas](./gotchas.md) (`Pins`) | Vize and Oxc own the semantic stack; analysis pins are exact and upgrades need fixture + compat evidence. |
| Crate boundaries, data flow, public APIs | [architecture](./architecture.md) (`Monorepo analysis pipeline`, `Stable boundary`, `Crate evolution`), [workspace crates index](../../docs/crates.md), [analysis-stack ADR](../../docs/adr/0001-analysis-stack.md), per-crate `crates/*/README.md` | End-to-end stage map; Oxc-taking APIs stay under `vue_vet_reactivity::oxc`; Vue Vet-owned types wrap third-party parsers. |
| Semantic IR layers (`ModuleSummary`, file-rule digests, source-contract fact families, lifetime facts) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Source-contract lanes`) | Small domain IRs — not a unified AST; file-rule reuse is digest-keyed; each fact family lists its runtime-evidence recipe. |
| Demand-gated source contracts (`customRef`, stopped `effectScope.run`, missing `toRefs` key, cached-result demand, private-field receivers, same-instance inject, VueUse ignore-window / shared first-instance args / cancelled `useDebounceFn`, JSON clone Date loss, history snapshot alias, `until` timeout, model defaults) | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`Demand proof is not source5 eligibility`, `Identifier escapes`, `Proxy identity`, `Collection escape-depth exhaustion is Unknown`), the rule essays under [`docs/rules/reactivity`](../../docs/rules/README.md) | Function-level region + source-order barriers; exact `@vueuse/core` / `@vueuse/shared` provenance; fresh native `Symbol()` is ancestor-absence proof; escape-depth exhaustion stays Unknown. Runtime premises via the `just oracle-*` recipes named in each entry. |
| Watcher / effect-scope lifetime rules (`no-watch-cleanup-current-source`, `no-nested-watch-without-cleanup`, `no-detached-effect-scope-without-stop`, `no-late-cancellation-guard`, `no-custom-ref-lost-notification`) | [architecture](./architecture.md) (`Semantic IR layers` → `lifetime`), [gotchas](./gotchas.md) (`Watch cleanup identity needs an executed schedule`, `once: true`, `customRef lost-notification`) | Shared lifetime index; acquisition before a later distinct allocation; abstain on `once: true` / non-closed options. |
| Derivation / scheduling practice rules (`prefer-stable-computed-identity`, `prefer-sync-ref-one-way`, `prefer-conditional-watch-source`, `prefer-queued-watch-flush`, `prefer-attached-effect-scope`, `prefer-lazy-computed-async`, `prefer-keyed-map-dependency`) | [architecture](./architecture.md) (`Semantic IR layers`), [conventions](./conventions.md) (`Rule contract`), [vue_vet_practice README](../../crates/vue_vet_practice/README.md) | Practice channel is fact-driven recipes, off-score; `just oracle-derivation-practice` / `oracle-scheduling-practice` / `oracle-computed-identity` carry the runtime premises. |
| Dirty-set / execution locality (`ChangeImpact`, `DirtyPlan`, work counters) | [architecture](./architecture.md) (`Performance model`, `Locality plan shape`), [gotchas](./gotchas.md) (`Template-only SFC edits re-run script analysis`, `Scheduling and identity`, `Sharing, not cloning`) | Dirty parse + warm linking/layer caches; prove cost with counters. Context epochs must not force re-parse. No generalized unified AST IR. |
| Reactivity graph facts, tracking scopes, module summaries, tracer completeness, or CLI reactivity digest | [reactivity tracer](./reactivity-tracer.md) (`Product stance`, `What "complete" means`, `ExportState policy algebra`), [gotchas](./gotchas.md) (`Reactivity tracer`), [literature matrix](./research/reactivity-tracer-literature.md), [science memo](./research/reactivity-tracer-science.md), [oracle](../../crates/vue_vet_reactivity/oracle/README.md), [JSON output](../../docs/json-output.md) | Static, under-approximating tracer; digest proves the tracer ran even when the score is clean; `REACTIVITY_GRAPH_VERSION` lives in `vue_vet_core::facts::graph`. Run-count claims use `just oracle-self-trigger`, not onTrack JSON. |
| Rule IDs, fixtures, source spans, deterministic output, configuration, or completion checks | [conventions](./conventions.md), [rule catalog](../../docs/rules/README.md) (`tracer` / `parity` / `practice` tiers), [gotchas](./gotchas.md) (`Spans and encoding`, `Configuration is part of diagnostic identity`) | Deliberate engineering rules contributors must preserve; catalog is generated by `just rules-catalog`. |
| Rule groups, `--list-rules`, `--group` selection, inventory vs scan-time enabling | [architecture](./architecture.md) (`Crate evolution` → `vue_vet_session`), [conventions](./conventions.md), [JSON output](../../docs/json-output.md) (`--list-rules`) | Product groups live in session; `RuleGroupId` slugs in core; effective config `off` for non-selected IDs. Counts come from the live registry, never prose. |
| Opt-in Vapor migration assessment (`category: migration`, `assessment` config, `Diagnostic.assessment`) | [architecture](./architecture.md) (`Semantic IR layers` → `vapor_migration`), [conventions](./conventions.md), [gotchas](./gotchas.md) (`Vapor migration assessment`, `<script vapor>` is setup), [technology stack](./technology-stack.md) (`Vue Vapor migration research`), [evidence page](../../docs/research/vapor-migration.md), [harness](../../research/vapor-migration/README.md) | Off-score like practice; `ready` = recommended now, `convertible` = possible at all; the admitted envelope widens only after a passing runtime fixture pair in the harness. |
| Shared rule fact queries (setup blocks, after-await calls, alias-aware write identity) | [architecture](./architecture.md) (`Crate evolution`), [vue_vet_rule_query README](../../crates/vue_vet_rule_query/README.md) | Workspace-internal query crate over Vue Vet facts; not a rule pack and not a published contract. |
| Safe edit producers, fix preview/application, atomic writes, or rollback | [conventions](./conventions.md) (`Edit contracts`), [edit model](../../docs/edit-model.md), [gotchas](./gotchas.md) (`Safe fixes need complete source coverage`), [technology stack](./technology-stack.md) (`atomic-write-file`) | Edits stay attached to active findings, validate before mutation, fail closed at the single-file limit. |
| npm launcher, native binaries, release tags, install matrix, pkg.pr.new previews | [technology stack](./technology-stack.md), [conventions](./conventions.md) (`npm launcher boundary`), [gotchas](./gotchas.md) (`Build, CI, release`), [install docs](../../docs/install.md) | Thin JS launcher only; platform packages, worktree-local Cargo targets, release/rollback rules, crates.io publish order. |
| Native binary size, benchmarks, precision corpus, or release quality gates | [quality gates](../../docs/quality-gates.md), [quality baselines](../../docs/quality-baselines.md), [conventions](./conventions.md) (`Performance regression checks`), [gotchas](./gotchas.md) (`Native-size budget`, `Benchmarks`) | Only the 3 % regression budget is enforced; rule growth is expected. `cargo bench` still unwinds; shipped CLI aborts. |
| Project session shared by CLI · LSP · MCP, `--explain`, `--explain-scope`, `--progress` | [architecture](./architecture.md) (`Reporting and edit planning`, `Thin editor host, LSP, and MCP`), [gotchas](./gotchas.md) (`Surfaces`), [JSON output](../../docs/json-output.md) (`--explain`, `--explain-scope`), crate READMEs for [session](../../crates/vue_vet_session/README.md), [lsp](../../crates/vue_vet_lsp/README.md), [mcp](../../crates/vue_vet_mcp/README.md) | Long-lived session owns config, cache, analyze, explain, path bounds; adapters stay thin; MCP never applies edits; progress is stderr-only. |
| Thin VS Code reactivity visualization | [`editors/vscode/README.md`](../../editors/vscode/README.md), [JSON output](../../docs/json-output.md) (`binding_nav`, `component_nav`) | Editor host shells out to CLI JSON; no analysis logic in TypeScript; `component_nav` is structural, not prop dataflow. |
| Vue JSX/TSX (`.jsx`/`.tsx`, SFC `lang=jsx|tsx`), render scopes, SFC macros | [architecture](./architecture.md) (`Semantic IR layers`), [gotchas](./gotchas.md) (`JSX and SFC macros`) | Oxc-owned JSX surface; no Babel transform; SFC-macro rules stay on `ScriptKind::Setup`. |
| Cache keys, baselines, diff mode, `node_modules` / symlink walks | [cache and diff guide](../../docs/cache-baseline-diff.md), [vue_vet_cache README](../../crates/vue_vet_cache/README.md), [gotchas](./gotchas.md) (`Cache`, `Paths are identities, not suffixes`) | Identity constants are hashed via `AnalysisStackIdentity::current()` and gated by `just compat-matrix`; diff mode keeps project findings. |
| Project import resolution, tsconfig / `.nuxt` paths, Nuxt / Vite auto-import maps, enrichment passes | [project graph](../../docs/project-graph.md), [architecture](./architecture.md) (`vue_vet_project pipeline`, `Analysis enrichment passes`, `Project intelligence`), [gotchas](./gotchas.md) (`Project graph and resolution`, `Nuxt / auto-import seeds and external follow`) | `oxc_resolver` bundler resolution; no Vite/Nuxt config execution; named `::run` enrichment steps, not AST Traverse or a JS plugin host. |
| Reactivity ecosystem API bags (Nuxt data, vue-i18n `t()`, …) | [architecture](./architecture.md) (`Reactivity tracer plugins`), [reactivity tracer](./reactivity-tracer.md) (`Tracer plugins`), [vue_vet_plugins README](../../crates/vue_vet_plugins/README.md) | Compile-time `TracerPlugin` / `NamedApiBag`; product auto-load; engine free of ecosystem names. |
| Current implementation order or delivery status | [GitHub tracker](https://github.com/alexzhang1030/vue-vet/issues/14), [root roadmap](../../ROADMAP.md) | Issues are the live execution plan; the roadmap records what is still ahead. |
| Product decisions (score formula, capability gating, no parallel engine) | [gotchas](./gotchas.md) (`Product decisions worth re-reading`), [goal](./goal.md) | Re-read before tuning rules to move the score or adding a second matching engine. |

Related records should be cross-linked. When a new record or important heading is added, update this map in the same change.
